#include "main.h"

/**
 * factorial - function
 * @n: parametre
 * Return: factorial of int
 */
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	if (n == 0)
	{
		return (1);
	}
	else
	{
		return (n * factorial(n - 1));
	}
}
