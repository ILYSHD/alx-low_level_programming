#include "main.h"

/**
 *  * _isupper - writes if c is uppercase
 *   * @dest: The character to test
 *    * Return: the result
 *     */
char *_strncat(char *dest, char *src, int n)
{
	return (dest);
}
