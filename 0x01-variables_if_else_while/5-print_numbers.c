#include <stdio.h>

/**
 * main - a program to print all the alphabet except q and e
 * Return: 0 (Success)
 */

int main(void)
{
	int d;

	for (d = 0; d < 10; ++d)
	{
		printf("%d", d);
	}
	printf("\n");
	return (0);
}
